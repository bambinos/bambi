# Prior { #bambi.Prior }

```python
Prior(name, auto_scale=True, dist=None, **kwargs)
```

Abstract specification of a term prior

## Parameters {.doc-section .doc-section-parameters}

<code>[**name**]{.parameter-name} [:]{.parameter-annotation-sep} [[str](`str`)]{.parameter-annotation}</code>

:   Name of prior distribution. Must be the name of a PyMC distribution (e.g., `"Normal"`, `"Bernoulli"`, etc.)

<code>[**auto_scale**]{.parameter-name} [:]{.parameter-annotation-sep} [[bool](`bool`)]{.parameter-annotation} [ = ]{.parameter-default-sep} [True]{.parameter-default}</code>

:   Whether to adjust the parameters of the prior or use them as passed. Default to `True`.

<code>[**dist**]{.parameter-name} [:]{.parameter-annotation-sep} [[pymc](`pymc`).[Distribution](`pymc.Distribution`) or [callable](`callable`) or None]{.parameter-annotation} [ = ]{.parameter-default-sep} [None]{.parameter-default}</code>

:   A callable that returns a valid PyMC distribution. The signature must contain `name`, `dims`, and `shape`, as well as its own keyworded arguments.

<code>[**kwargs**]{.parameter-name} [:]{.parameter-annotation-sep} [[dict](`dict`)]{.parameter-annotation} [ = ]{.parameter-default-sep} [{}]{.parameter-default}</code>

:   Optional keywords specifying the parameters of the named distribution.

## Methods

| Name | Description |
| --- | --- |
| [update](#bambi.Prior.update) | Update the arguments of the prior with additional arguments |

### update { #bambi.Prior.update }

```python
Prior.update(**kwargs)
```

Update the arguments of the prior with additional arguments

#### Parameters {.doc-section .doc-section-parameters}

<code>[**kwargs**]{.parameter-name} [:]{.parameter-annotation-sep} [[dict](`dict`)]{.parameter-annotation} [ = ]{.parameter-default-sep} [{}]{.parameter-default}</code>

:   Optional keyword arguments to add to prior args.